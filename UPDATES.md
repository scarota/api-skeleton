## Updates to run the app

1. I had to install setuptools to run the app. Mashmallow requires the distutils package which is removed in Python 3.12. setuptools provides a compatible version of distutils.
